---
"@onrampfunds/embed": minor
"@onrampfunds/embed-react": minor
---

Accept a `data` promise in `mount()` and as a React prop: the card waits for your fetch and renders on settle — silently by default, with the themed skeleton via `state: 'mounting'`. Direct config is unchanged and remains the primitive.
